<h1>List of Users</h1>

  <% @users.each do |user| %>
    <%= render user %>
    <p>
      <%= link_to "Detail of User", user %>
    </p>
    <p>
      <%= link_to "Edit User's Data", edit_user_path(user)%>
    </p>
  <% end %>

<p>
  <%= link_to "Create new article", signup_path %>
</p>